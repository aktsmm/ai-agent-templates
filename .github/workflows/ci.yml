name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-customer-support:
    name: "Test: Customer Support"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        working-directory: templates/customer-support
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with Ruff
        working-directory: templates/customer-support
        run: |
          ruff check src/ tests/

      - name: Run tests
        working-directory: templates/customer-support
        run: |
          pytest -v --tb=short

  test-subsidy-consultant:
    name: "Test: Subsidy Consultant"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        working-directory: templates/subsidy-consultant
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with Ruff
        working-directory: templates/subsidy-consultant
        run: |
          ruff check src/ tests/

      - name: Run tests
        working-directory: templates/subsidy-consultant
        run: |
          pytest -v --tb=short

  test-ecommerce-assistant:
    name: "Test: E-commerce Assistant"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        working-directory: templates/ecommerce-assistant
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with Ruff
        working-directory: templates/ecommerce-assistant
        run: |
          ruff check src/ tests/

      - name: Run tests
        working-directory: templates/ecommerce-assistant
        run: |
          pytest -v --tb=short

  test-legal-document-analyzer:
    name: "Test: Legal Document Analyzer"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        working-directory: templates/legal-document-analyzer
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with Ruff
        working-directory: templates/legal-document-analyzer
        run: |
          ruff check src/ tests/

      - name: Run tests
        working-directory: templates/legal-document-analyzer
        run: |
          pytest -v --tb=short

  test-sales-lead-qualifier:
    name: "Test: Sales Lead Qualifier"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        working-directory: templates/sales-lead-qualifier
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with Ruff
        working-directory: templates/sales-lead-qualifier
        run: |
          ruff check src/ tests/

      - name: Run tests
        working-directory: templates/sales-lead-qualifier
        run: |
          pytest -v --tb=short

  test-it-helpdesk:
    name: "Test: IT Helpdesk"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        working-directory: templates/it-helpdesk
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with Ruff
        working-directory: templates/it-helpdesk
        run: |
          ruff check src/ tests/

      - name: Run tests
        working-directory: templates/it-helpdesk
        run: |
          pytest -v --tb=short

  test-hr-onboarding:
    name: "Test: HR Onboarding"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        working-directory: templates/hr-onboarding
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with Ruff
        working-directory: templates/hr-onboarding
        run: |
          ruff check src/ tests/

      - name: Run tests
        working-directory: templates/hr-onboarding
        run: |
          pytest -v --tb=short

  test-content-marketing:
    name: "Test: Content Marketing"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        working-directory: templates/content-marketing
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint with Ruff
        working-directory: templates/content-marketing
        run: |
          ruff check src/ tests/

      - name: Run tests
        working-directory: templates/content-marketing
        run: |
          pytest -v --tb=short
